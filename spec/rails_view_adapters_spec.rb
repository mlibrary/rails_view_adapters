require "spec_helper"

describe RailsViewAdapters do
  it "has a version number" do
    expect(RailsViewAdapters::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
